- Run as daemon

export LANGUAGE=en_US.UTF-8
export LANG=en_US.UTF-8
export LC_ALL=en_US.UTF-8
sudo numactl --interleave=all $MONGO_PATH/bin/mongod --dbpath $MONGO_DB_PATH --fork --auth --rest  --logpath $MONGO_LOG/mongodb.log

- Or as a normal process

sudo $MONGO_PATH/bin/mongod --dbpath $MONGO_DB_PATH --auth

- Start webservices with gunicorn

in $HOME/provenance_api/src/prov-services/src/
nohup ./start_flask_gunicorn.sh
